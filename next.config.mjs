/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {hostname:"secret-goldfinch-855.convex.cloud"}
        ]
    }
    
};

export default nextConfig;

